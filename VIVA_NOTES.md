
# Viva Notes — What to say & show

## Key files to open:
- app/api/recipe/route.js   -> AI call + prompt engineering
- app/page.jsx             -> UI + fetch to API route
- components/RecipeCard.jsx -> Displays generated recipe
- utils/ai.js              -> Helper that wraps OpenAI calls
- package.json             -> dependencies and scripts

## Short script (20–30 seconds)
"My project Auto-Chef AI uses Next.js for frontend and serverless API routes. The AI integration uses the OpenAI API (GPT model) via a backend route where the prompt is constructed and sent; the LLM returns structured recipe text that the frontend displays. I used Vercel to deploy with serverless functions."

## If asked about AI assistance:
"I used Lovable AI to scaffold the project but I wrote and modified the AI prompts, integrated API keys, adjusted UI, and handled deployment and testing myself."

