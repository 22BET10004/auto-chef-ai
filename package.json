{
  "name": "nutrition-planner-web-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && mkdir -p dist && cp -r .next/* dist/ 2>/dev/null && touch dist/index.html",
    "build:dev": "next build && mkdir -p dist && cp -r .next/* dist/ 2>/dev/null && touch dist/index.html",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "jspdf": "^3.0.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1",
    "next": "14.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "recharts": "^3.2.1"
  }
}